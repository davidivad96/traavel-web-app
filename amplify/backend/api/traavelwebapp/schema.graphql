type User @model @auth(rules: [{ allow: public }]) {
  id: ID!
  email: AWSEmail!
  name: String
  avatarUrl: AWSURL
  plans: [Plan] @hasMany(indexName: "byUser", fields: ["id"])
}

type Plan @model @auth(rules: [{ allow: public }]) {
  id: ID!
  name: String!
  destination: String
  startDate: AWSDate
  endDate: AWSDate
  owner: User @belongsTo(fields: ["ownerId"])
  ownerId: ID! @index(name: "byUser")
}
