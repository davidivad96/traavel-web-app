type Plan @model @auth(rules: [{allow: public}]) {
  id: ID!
  owner: User @hasOne
  name: String!
  destination: String
  startDate: AWSDate
  endDate: AWSDate
}

type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  email: AWSEmail!
  name: String
  avatarUrl: AWSURL
}
 